// FIBONACCI Entre los números 0 y 1000:

/*
function fibonacci(limit) {
  const arrFib = [0, 1];
  for (let i = 2; i <= limit; i++) {
    arrFib.push(arrFib[i - 1] + arrFib[i - 2]);
  }
  return arrFib;
}

const testFib = fibonacci(16);
console.log(testFib);
*/

// FIBONACCI Números pares entre 0 y 1000:

/*
function numerosPares() {
  for (let index = 1; index <= 1000; index ++) {
    if (index % 2 === 0) {
      console.log(index);
    }
  }
}

numerosPares();
*/


// FIBONACCI Números impares entre 0 y 1000:

/*
function numerosImpares() {
  for (let i = 1; i <= 1000; i++) {
    if (i % 2 !== 0) {
      console.log(i);
    }
  }
}

numerosImpares();
*/

